import { SpacedTextPipe } from './spaced-text.pipe';

describe('SpacedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SpacedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
